{
  "name": "mcp-server-chart-option",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf build/*",
    "build": "npm run clean && tsc && tsc-alias -p tsconfig.json",
    "start": "npm run build && npx @modelcontextprotocol/inspector node build/index.js"
  },
  "bin": {
    "mcp-server-chart-option": "./build/index.js"
  },
  "keywords": [
    "mcp",
    "chart",
    "echarts"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.17.2",
    "@types/node": "^24.10.0",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.3"
  }
}
